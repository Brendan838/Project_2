
<div id = "div1">

    <div id = "div2">

   <h1 id="snipHead">Saved Snippets</h1>
        <button class="saveButton folders"> <a href= "snips">New Snippet</a> </button>
         {{#each postData as |post|}}
        <button class="saveButton folders"> <a href = "/snippet/{{post.id}}">{{post.title}}</a></button>
          {{/each}}
    </div>

    <div id = "div3">
    <h1 id = "codeEnterHead" >Enter Code Here <button class="saveButton logOuts" id="logout1">Logout</button></h1>

    <section class= "flex-Column">
	  <textarea class="newCode" rows="1" columns="50" id= "snippetTitle"> </textarea>
    <textarea class="newCode" rows="9" columns="50" id="snippetBody"> </textarea>
    </section>

 <span class="mif-copy mif-5x copyIcons" id= "copyBtn"></span>
<span class="mif-paste mif-5x copyIcons" id= "pasteBtn"></span>



    <button class="saveButton" id="saveBtn"> Save </button>
    
      
    </div>

</div>

<script type="text/javascript" src="/js/create.js"></script>

nav  <span class="mif-copy mif-3x" id= "copyBtn"></span>  <span class="mif-copy mif-3x" id= "copyBtn"></span>